# Factory completion, 2026-09-18

User authorization: ergänze alles was fehlt. Implements the existing factory specification and the agreed missing scripts/tool loops; no outreach, no paid live calls without a suitable authorized budget. Preserve source truth and all approval, reservation and attempt limits.

Architecture: keep seven stage contracts and deterministic renderer. Add bounded tool-assisted research before model stages, explicit operator-supplied evidence about business activity, durable event logs, supported CLI/script entry points and batch execution. Do not label deterministic scripts as autonomous reasoning, and do not label fixture validation as live success.

- [x] Crawler: reproduce timeout failures; capture loaded documents despite network-idle timeout, distinguish limitations; isolate Lighthouse failure from coordinator process, preserve egress policy and limits; tests.
- [x] Research tools: bounded search adapter with explicit configuration, public-source fetching with existing safe crawler, grounded business-status evidence, resumable persisted discovery jobs and tool events; tests with fake transports.
- [x] Runtime: bounded decision/action/observation loop wired to paid ModelProvider accounting for every decision call, persist results and errors, strict tools and validated plans, no arbitrary code; CLI commands and real fixture integration tests.
- [x] Coordinator: executable setup/check/analyse/review/approve/resume/preview/batch scripts, durable operator business verification with source URLs, user-facing review files and trace inspection, doc of real limits.
- [x] Verification: full build/lint/test, actual browser fixture E2E, one free live crawl smoke test, independent review; sync code to private GitHub snapshot preserving .env and original databases.

Existing renderer, browser checks, qualification and repair loops already exist. Inspect/reuse these instead of implementing parallel fake versions. Unknown facts remain unknown. Current user instruction approves implementation; it does not expand the prior review-only USD 5 budget to new demo/tool calls. Manual Anton demo is not a factory completion benchmark.
